const getters = {
    // getCountry: (state, rootState) => id => {
    //     console.log("rootState", )
    //     const userCountry = state.countries.find(country => country.country_id === id);
    //     return userCountry.id;
    // }
};

export default getters;